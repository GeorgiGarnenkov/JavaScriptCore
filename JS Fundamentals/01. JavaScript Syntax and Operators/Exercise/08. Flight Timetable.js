function solve([state, townName, time, flightNum, gateNum]) {
    
    console.log(`${state}: Destination - ${townName}, Flight - ${flightNum}, Time - ${time}, Gate - ${gateNum}`);
}

solve(['Departures', 'London', '22:45', 'BR117', '42']);